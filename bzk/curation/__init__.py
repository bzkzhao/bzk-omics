"""Curation records — the one non-derivable input (`ARCHITECTURE.md` §2, §3).

Everything else in the graph is regenerable from `raw/` plus this (I9). `data/curation/*.json` is
human judgement about experiments this laboratory did not perform, and `loader.py` turns one such
record into a change-set — or refuses, naming what the curator still owes.
"""
