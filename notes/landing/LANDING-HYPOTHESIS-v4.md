# LANDING — `HYPOTHESIS.md` v4

**Landed 2026-09-20 from bzk's own copy, unchanged.** Its sha256 is
`ae05ddfc208f1e56d4e3313a475c026f31afa6279cc56d4c750c51f8240cbf6c`, and the
repository copy must hash the same. This note exists so the document itself
stays exactly as its author registered it.

## Which version, and why this one

**v4 is current (bzk, 2026-09-20).** The version history, as stated by its
author:
- **v1 and v2** were registered versions.
- **v3 was a working draft for testing and was never formally published.**
- **v4 supersedes both.** Its "Changelog from v3" records changes against that
  draft. Its "Changelog from v2" is carried from it.

## What this corrects

- **`notes/reports/HANDOFF-2026-09-19.md` §7 says to land `HYPOTHESIS.md` v2.**
  Superseded: v4 is what lands. That handoff is a dated record and is not edited
  in place.
- **The same handoff says the findings document cites `_AMENDED` and v3,
  *"neither of which was ever produced"*.** For v3 this is **true in the sense
  that matters**: v3 was never formally published. But a working draft did
  exist. The findings document's citation of v3 is therefore a citation of an
  unregistered draft, not of a version that never existed. `_AMENDED` is not
  addressed here.

## What follows from landing it

v4 governs what counts as an instance, and two of its passages bear directly on
the next measurement:
- **D5 and H9 register a weakening direction.** It is *"a deposit whose methods
  specify the test fully, where the reconstruction's support matches"*.
  `PXD026748`'s methods state the software, the model and the selection rule,
  and leave only the imputation unstated
  (`notes/reports/REVIEW-ADR-0035.md`, addendum). The reconstruction of that
  deposit is therefore close to the test v4 asks for, not a replication of the
  anchor's D5.
- **H9's *sharp form*, a family of defensible reconstructions, is marked *"not
  yet registered"*,** with the instruction *"Register it before measuring it."*
  So a v5 amendment registering it precedes the reconstruction's
  pre-registration.
